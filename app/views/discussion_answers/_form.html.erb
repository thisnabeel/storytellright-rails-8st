<%= form_for(@discussion_answer) do |f| %>
  <% if @discussion_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discussion_answer.errors.count, "error") %> prohibited this discussion_answer from being saved:</h2>

      <ul>
      <% @discussion_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :discussion_question_id %><br>
    <%= f.number_field :discussion_question_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
