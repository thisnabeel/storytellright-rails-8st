<%= form_for(@discussion_question) do |f| %>
  <% if @discussion_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@discussion_question.errors.count, "error") %> prohibited this discussion_question from being saved:</h2>

      <ul>
      <% @discussion_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :position %><br>
    <%= f.number_field :position %>
  </div>
  <div class="field">
    <%= f.label :discussion_id %><br>
    <%= f.text_field :discussion_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
