<div class="card discussion">
    <h4 class="card-header">
    <span class="delete-discussion fa fa-trash" data-id="<%= discussion.id %>"></span>
    <span contentEditable key="title" url="/discussions/<%= discussion.id %>">
        <%= discussion.title %>
    </span>
    <span class="duplicate-discussion fa fa-clone" data-id="<%= discussion.id %>"></span>

    <a class="btn btn-primary pull-right" href="/discussions/<%= discussion.id %>">Enter</a>
    </h4>
    <div class="card-body hidden">
    <h5 class="card-title">Special title treatment</h5>
    <p class="card-text">With supporting text below as a natural lead-in to additional content.</p>
    <a href="#" class="btn btn-primary">Go somewhere</a>
    </div>
</div>